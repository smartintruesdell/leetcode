export class Solution {
    static solve(input: string): string {
        // TODO: Implement solution
        return input;
    }
}

// For running directly
if (require.main === module) {
    console.log(Solution.solve("test input"));
}
